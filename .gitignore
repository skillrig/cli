# SpecLedger issue store lock
issues.jsonl.lock

# Pycache (skills eval)
__pycache__/

# Go build artifacts
/skillrig
*.exe
*.test
*.out

# OS / editor cruft
.DS_Store
*.swp

# Secrets / captured traffic — NEVER commit (Qodo API token; HAR dumps may embed bearer tokens; GitHub App private keys)
qodo-auth.key
*.key
*.pem
*.har

# GoReleaser output
/dist/

# Skill eval results (generated output; the eval-set inputs stay tracked)
.agents/skills/qodo-manage-rules/evals/direct-probe-result.json
