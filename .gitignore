# SpecLedger issue store lock
issues.jsonl.lock

# Pycache (skills eval)
__pycache__/

# Go build artifacts
/skillrig
*.exe
*.test
*.out

# OS / editor cruft
.DS_Store
*.swp

# Secrets / captured traffic — NEVER commit (Qodo API token; HAR dumps may embed bearer tokens)
qodo-auth.key
*.key
*.har
